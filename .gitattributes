# Крајовите на редовите се LF во складиштето, без разлика на подесувањата на
# поединечниот компјутер (core.autocrlf=true на Windows инаку ја претвора цела
# датотека во CRLF и секој ред се прикажува како изменет во diff-от).
* text=auto eol=lf

# Бинарни — git да не ги допира воопшто.
*.png binary
*.zip binary
